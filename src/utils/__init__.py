"""Utility functions and helper modules.

This package contains pure utility functions without side effects: data
converters, formatters, mappers, and other stateless transformations.

No module in this package should depend on external systems (databases,
HTTP clients, file I/O) except via explicit function parameters.
"""
