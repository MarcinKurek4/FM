"""Test suite for the FM project.

This package contains unit and integration tests for all modules in the
``src`` package. Tests are organised by layer (models, utils, repositories,
services) and by type (unit vs integration).

Run all tests::

    poetry run pytest

Run with coverage report::

    poetry run pytest --cov=src --cov-report=term-missing
"""
